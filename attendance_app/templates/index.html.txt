<!DOCTYPE html>
<html>
<head><title>Attendance</title></head>
<body>
<h1>Mark Attendance</h1>
<form method="POST" action="/mark_attendance">
    <table border="1">
        <tr><th>Name</th><th>Salary</th><th>Status</th><th>Edit</th></tr>
        {% for emp in employees %}
        <tr>
            <td>{{ emp.name }}</td>
            <td>{{ emp.salary }}</td>
            <td>
                <select name="{{ emp.id }}">
                    <option value="present">Present</option>
                    <option value="absent">Absent</option>
                </select>
            </td>
            <td><a href="/edit_salary/{{ emp.id }}">Edit Salary</a></td>
        </tr>
        {% endfor %}
    </table>
    <br>
    <button type="submit">Submit</button>
</form>
<a href="/dashboard">View Dashboard</a>
</body>
</html>
